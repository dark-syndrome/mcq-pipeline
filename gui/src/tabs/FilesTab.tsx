import TabShell from '../components/TabShell'

export default function FilesTab() {
  return (
    <TabShell
      title="Files"
      subtitle="Query SQLite/Supabase with a graphical filter builder. Export accepted questions as JSON, DOCX, or PDF."
      session="Sessions 7 & 8"
    />
  )
}
