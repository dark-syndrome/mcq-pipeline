import TabShell from '../components/TabShell'

export default function RunTab() {
  return (
    <TabShell
      title="Run"
      subtitle="Upload a .md source, configure run parameters, and execute the pipeline with live stage-by-stage progress."
      session="Sessions 3 & 4"
    />
  )
}
