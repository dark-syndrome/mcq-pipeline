import TabShell from '../components/TabShell'

export default function ModelTab() {
  return (
    <TabShell
      title="Model"
      subtitle="Edit all config.yaml parameters via a structured form. Save and load named config profiles."
      session="Session 5"
    />
  )
}
